<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to wiki.title, wiki_path(wiki) %> <small>created: <%= wiki.created_at.strftime("%m/%d/%Y") %></small>
    </h4>
  </div>
</div>
